{
  "name": "electron",
  "version": "1.0.0",
  "main": "main.js",
  "license": "MIT",
  "scripts": {
    "start": "nodemon --exec electron . --watch ./ --ext .js,.html,.css,.vue",
    "update": "git add . && git commit -m 'update' && git push"
  },
  "devDependencies": {
    "electron": "^23.1.3"
  }
}
